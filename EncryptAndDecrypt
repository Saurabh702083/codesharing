/**
 * Class Name: AdminMasterService
 * *
 * Author: V1018841(Saurabh Mahto)
 * <p>
 * Copyright (c) 2026 [State Bank of India]
 * All rights reserved
 * *
 * Version:1.0
 */
@Service
@RequiredArgsConstructor
public class AdminMasterService {
    private final LoggerUtility logger = LoggerFactoryUtility.getLogger(this.getClass());
    private final MasterHandlerFactory<BaseMasterDto> masterHandlerFactory;
    private final MasterWorkFlowService masterWorkFlowService;
    private final MasterBulkUploadService masterBulkUploadService;
    private final FileService fileService;
    private final AdminPortalConfig adminPortalConfig;
    private final MasterBulkUploadValidation bulkUploadValidation;

    @Transactional
    public AdminPortalResponse<String> create(MasterRequest masterRequest, String masterCode) {
        BaseMasterService<BaseMasterDto> masterService = resolveMasterService(masterCode);

        BaseMasterDto masterDto = masterService.mapToMasterDto(masterRequest);
        logger.info("Create master request :{}", masterDto);

        masterService.validate(masterDto, WorkflowOperation.CREATE);
        logger.info("Request validated successfully.");

        masterDto = masterService.save(masterDto, WorkflowOperation.CREATE);
        logger.info("Record Created successfully for master code: {}", masterDto.getCode());

        MasterWorkFlowDto masterWorkFlowDto = masterService.mapToMasterWorkFlowDto(masterDto, WorkflowOperation.CREATE, MessageFormat.format(MASTER_ACTION_PERFORMED, WorkflowOperation.CREATE.name()));
        masterWorkFlowService.save(masterWorkFlowDto);
        logger.info("Master flow record updated for :{}", masterDto.getCode());

        return mapToResponse(MessageFormat.format(MASTER_ACTION_PERFORMED, WorkflowOperation.CREATE.name()));
    }

    @Transactional
    public AdminPortalResponse<String> update(MasterRequest masterRequest, String masterCode) {
        BaseMasterService<BaseMasterDto> masterService = resolveMasterService(masterCode);

        BaseMasterDto masterDto = masterService.mapToMasterDto(masterRequest);
        logger.info("Create master request :{}", masterDto);

        masterService.validate(masterDto, WorkflowOperation.UPDATE);
        masterDto = masterService.save(masterDto, WorkflowOperation.UPDATE);
        logger.info("Record update successfully for master code: {}", masterDto.getCode());

        MasterWorkFlowDto masterWorkFlowDto = masterService.mapToMasterWorkFlowDto(masterDto, WorkflowOperation.UPDATE, MessageFormat.format(MASTER_ACTION_PERFORMED, WorkflowOperation.UPDATE.name()));
        BaseMasterDto savedMasterDto = masterService.findByIds(List.of(masterDto.getId())).getFirst();
        masterWorkFlowService.update(masterWorkFlowDto, savedMasterDto, masterDto);
        logger.info("Master flow record updated for :{}", masterDto.getCode());

        return mapToResponse(MessageFormat.format(MASTER_ACTION_PERFORMED, WorkflowOperation.UPDATE.name()));
    }

    @Transactional(readOnly = true)
    public AdminPortalResponse<Map<String, Object>> search(String masterCode, MasterSearchRequest request, Pageable pageable) {
        BaseMasterService<BaseMasterDto> masterService = resolveMasterService(masterCode);
        Page<BaseMasterDto> page = masterService.search(request, pageable);
        return masterService.mapToResponse(page.getContent(), page.getTotalElements());
    }

    @Transactional
    public AdminPortalResponse<String> approve(MasterBulkRequest request, String masterCode) {
        return performAction(request, WorkflowOperation.APPROVE, List.of(MasterAction.PENDING_FOR_APPROVAL.name(), MasterAction.PENDING_FOR_INACTIVE.name(),  MasterAction.PENDING_FOR_REJECTED.name()), masterCode);
    }

    @Transactional
    public AdminPortalResponse<String> reject(MasterBulkRequest request, String masterCode) {
        return performAction(request, WorkflowOperation.REJECT, List.of(MasterAction.PENDING_FOR_APPROVAL.name(), MasterAction.PENDING_FOR_INACTIVE.name()), masterCode);
    }

    @Transactional
    public AdminPortalResponse<String> inactive(MasterBulkRequest request, String masterCode) {
        return performAction(request, WorkflowOperation.INACTIVE, List.of(MasterAction.APPROVED.name()), masterCode);
    }

    private AdminPortalResponse<String> performAction(MasterBulkRequest request, WorkflowOperation performOperation, List<String> allowedPreviousActions, String masterCode) {
        BaseMasterService<BaseMasterDto> masterService = resolveMasterService(masterCode);
        masterService.validateAction(request, performOperation.name());

        List<BaseMasterDto> masters = masterService.findByIds(request.getMasterIds());
        masterService.validateBulkMasters(masters, allowedPreviousActions, performOperation.name());
        logger.info("Request validated successfully for operation : {}.", performOperation.name());

        masterService.performAction(request.getMasterIds(), performOperation);
        logger.info(" {} : Operation performed successfully for master code: {}", performOperation.name(), masterCode);

        String remarks = StringUtils.isNotEmpty(request.getRemarks()) ? request.getRemarks() : MessageFormat.format(MASTER_ACTION_PERFORMED, performOperation.name());
        List<MasterWorkFlowDto> masterWorkFlowDtos = masters.stream().map(master -> masterService.mapToMasterWorkFlowDto(master, performOperation, remarks)).toList();
        masterWorkFlowService.save(masterWorkFlowDtos);
        logger.info("Master flow record saved of :{}, for Operation : {}", masterCode, performOperation.name());

        return mapToResponse(MessageFormat.format(MASTER_ACTION_PERFORMED, performOperation.name()));
    }


    private AdminPortalResponse<String> mapToResponse(String s) {
        return AdminPortalResponse.<String>builder().status(SUCCESS_RESPONSE_CODE).data(List.of(s)).count(1L).total(1L).build();
    }

    /**
     * This is to bulk upload the master data via a cvc file.
     * @param masterCode String
     * @param file multiPartFile
     * @return MasterBulkUploadResponse
     */
    public AdminPortalResponse<MasterBulkUploadResponse> upload(String masterCode, MultipartFile file) {
        BaseMasterService<BaseMasterDto> masterService = resolveMasterService(masterCode);
        MasterBulkUploadConfig config= masterBulkUploadService.findConfigByMasterType(masterCode);
        // validate file if csv or not
        String checksum = masterBulkUploadService.generateChecksum(file);
        bulkUploadValidation.validateFile(file, config,checksum);
        // get master config and check if bulk upload is allowed or not.
        BulkUploadFormatConfig formatConfig  =stringToDto(config.getFileFormatConfig(),BulkUploadFormatConfig.class);
        // Upload file on s3
        String s3Path= fileService.uploadFile(file);
        //insert file details in masterBulkUploadFile Table.
        MasterBulkUploadFile uploadFile= masterBulkUploadService.insertFileRecord(masterCode, file,s3Path,config.getId(), checksum);
        try {
            List<Map<String, String>> records = masterBulkUploadService.readRecords(file, formatConfig);
            processUploadRecords(masterCode, masterService, formatConfig, uploadFile, records);
            return buildUploadResponse(uploadFile);
        } catch (AdminPortalException  ex) {
            markUploadAsFailed(uploadFile, ex.getMessage());
            throw ex;
        }
    }

    private void markUploadAsFailed(MasterBulkUploadFile uploadFile, String remark) {
        uploadFile.setParsingStatus(BulkUploadParsingStatus.FAILED);
        uploadFile.setFileStatus(BulkUploadFileStatus.FAILED);
        String remarkMessage= MDC.get(CORRELATION_ID)+"||"+StringUtils.truncate(remark,800);
        uploadFile.setRemark(StringUtils.defaultIfBlank(remarkMessage, "Bulk upload failed."));
        masterBulkUploadService.updateFileRecord(uploadFile);
    }

    private BaseMasterService<BaseMasterDto> resolveMasterService(String masterCode) {
        MasterType masterType = MasterType.getMasterType(masterCode);
        logger.info("Master request for: {}", masterType);
        return masterHandlerFactory.getMasterService(masterType.getCode());
    }

    private void processUploadRecords(String masterCode, BaseMasterService<BaseMasterDto> masterService, BulkUploadFormatConfig formatConfig, MasterBulkUploadFile uploadFile, List<Map<String, String>> records) {
        long successCount = 0;
        long failedCount = 0;
        int batchSize = adminPortalConfig.getMasterBulkUploadBatchSize();
        for (int start = 0; start < records.size(); start += batchSize) {
            int end = Math.min(start + batchSize, records.size());
            List<Map<String, String>> batchRecords = records.subList(start, end);
            BulkUploadProcessResult processResult = processBatch(masterCode, formatConfig, masterService, batchRecords);
            List<BulkUploadRowResult> rowResults = processResult.getRowResults();
            for (int index = 0; index < batchRecords.size(); index++) {
                BulkUploadRowResult rowResult = rowResults.get(index);
                if (BulkUploadRecordStatus.SUCCESS.name().equals(rowResult.getRecordStatus())) {
                    successCount++;
                } else {
                    failedCount++;
                }
            }
            if (processResult.getWorkflowDtos() != null && !processResult.getWorkflowDtos().isEmpty()) {
                masterWorkFlowService.save(processResult.getWorkflowDtos());
            }
            masterBulkUploadService.saveMasterFileDtls(buildUploadFileDetails(uploadFile, batchRecords, rowResults));
        }
        updateMasterFile(uploadFile, records, successCount, failedCount);
        logger.info("Bulk upload completed for master code: {}. total: {}, success: {}, failed: {}", masterCode, records.size(), successCount, failedCount);
    }

    private void updateMasterFile(MasterBulkUploadFile uploadFile, List<Map<String, String>> records, long successCount, long failedCount) {
        uploadFile.setTotalRecords((long) records.size());
        uploadFile.setSuccessRecords(successCount);
        uploadFile.setFailedRecords(failedCount);
        uploadFile.setParsingStatus(BulkUploadParsingStatus.COMPLETED);
        uploadFile.setFileStatus(failedCount > 0 ? BulkUploadFileStatus.COMPLETED_WITH_ERRORS : BulkUploadFileStatus.COMPLETED);
        uploadFile.setRemark(String.format("Bulk upload processed. Success records: %d, Failed records: %d", successCount, failedCount));
        masterBulkUploadService.updateFileRecord(uploadFile);
    }

    private AdminPortalResponse<MasterBulkUploadResponse> buildUploadResponse(MasterBulkUploadFile uploadFile) {
        MasterBulkUploadResponse data = MasterBulkUploadResponse.builder().uploadFileId(uploadFile.getId()).masterType(uploadFile.getMasterType()).filePath(uploadFile.getFilePath()).totalRecords(uploadFile.getTotalRecords()).successRecords(uploadFile.getSuccessRecords()).failedRecords(uploadFile.getFailedRecords()).parsingStatus(String.valueOf(uploadFile.getParsingStatus())).fileStatus(String.valueOf(uploadFile.getFileStatus())).remark(uploadFile.getRemark()).build();
        return AdminPortalResponse.<MasterBulkUploadResponse>builder().status(SUCCESS_RESPONSE_CODE).data(List.of(data)).count(1L).total(1L).build();
    }


    private BulkUploadProcessResult processBatch(String masterType, BulkUploadFormatConfig formatConfig, BaseMasterService<BaseMasterDto> handler, List<Map<String, String>> records) {
        List<BulkUploadRecordContext> contexts = records.stream().map(record -> BulkUploadRecordContext.builder().masterCode(masterType).record(record).formatConfig(formatConfig).build()).toList();
        try {
            return processRecords(handler, contexts);
        } catch (AdminPortalException ex) {
            return BulkUploadProcessResult.builder().rowResults(contexts.stream().map(context -> failureResult(ex.getMessage())).toList()).workflowDtos(List.of()).build();
        }
    }

    private BulkUploadProcessResult processRecords(BaseMasterService<BaseMasterDto> handler, List<BulkUploadRecordContext> contexts) {
        List<BulkUploadRowResult> rowResults = new ArrayList<>(Collections.nCopies(contexts.size(), null));
        List<MasterWorkFlowDto> workflowDtos = new ArrayList<>();
        List<CreateMaster> createMasters = new ArrayList<>();
        List<CreateInactivation> createInactivation = new ArrayList<>();

        for (int index = 0; index < contexts.size(); index++) {
            BulkUploadRecordContext context = contexts.get(index);
            BaseMasterDto dto = toDto(context.getRecord(), handler.getDtoClass());
            String status = context.getRecord().get(STATUS);

            try {
                if (StringUtils.isBlank(status) || ACTIVE.equalsIgnoreCase(status)) {
                    handler.validate(dto, WorkflowOperation.CREATE);
                    if (handler.findExisting(dto).isPresent()) {
                        rowResults.set(index, failureResult(RECORD_ALREADY_EXISTS));
                    } else {
                        createMasters.add(new CreateMaster(index, dto));
                    }
                    continue;
                }

                if (INACTIVE.equalsIgnoreCase(status)) {
                    Optional<BaseMasterDto> activeRecord = handler.findActiveApproved(dto);
                    if (activeRecord.isPresent()) {
                        createInactivation.add(new CreateInactivation(index, activeRecord.get()));
                    } else {
                        rowResults.set(index, failureResult(NO_VALID_RECORDS));
                    }
                    continue;
                }

                rowResults.set(index, failureResult("Unsupported status value: " + status));
            } catch (AdminPortalException ex) {
                rowResults.set(index, failureResult(ex.getMessage()));
            }
        }

        createMaster(handler, createMasters, rowResults, workflowDtos);
        createInactivation(handler, createInactivation, rowResults, workflowDtos);
        return BulkUploadProcessResult.builder().rowResults(rowResults).workflowDtos(workflowDtos).build();
    }

    private void createMaster(BaseMasterService<BaseMasterDto> handler, List<CreateMaster> createMasters, List<BulkUploadRowResult> rowResults, List<MasterWorkFlowDto> workflowDtos) {
        if (createMasters.isEmpty()) {
            return;
        }

        List<BaseMasterDto> dtos = createMasters.stream().map(CreateMaster::dto).toList();
        List<BaseMasterDto> savedDtos = handler.saveAll(dtos);
        for (int i = 0; i < createMasters.size(); i++) {
            CreateMaster createMaster = createMasters.get(i);
            BaseMasterDto savedDto = savedDtos.get(i);
            workflowDtos.add(handler.mapToMasterWorkFlowDto(savedDto, WorkflowOperation.CREATE, MessageFormat.format("Bulk upload {0} action performed.", WorkflowOperation.CREATE.name())));
            rowResults.set(createMaster.index(), BulkUploadRowResult.builder().recordStatus(BulkUploadRecordStatus.SUCCESS.name()).operation(WorkflowOperation.CREATE.name()).remark(CREATE_PENDING_REMARK).masterRecordId(savedDto.getId()).build());
        }
    }

    private void createInactivation(BaseMasterService<BaseMasterDto> handler, List<CreateInactivation> createInactivations, List<BulkUploadRowResult> rowResults, List<MasterWorkFlowDto> workflowDtos) {
        if (createInactivations.isEmpty()) {
            return;
        }
        handler.performAction(createInactivations.stream().map(createInactivation -> createInactivation.record().getId()).toList(), WorkflowOperation.INACTIVE);
        for (CreateInactivation createInactivation : createInactivations) {
            BaseMasterDto record = createInactivation.record();
            workflowDtos.add(handler.mapToMasterWorkFlowDto(record, WorkflowOperation.INACTIVE, MessageFormat.format("Bulk upload {0} action performed.", WorkflowOperation.INACTIVE.name())));
            rowResults.set(createInactivation.index(), BulkUploadRowResult.builder().recordStatus(BulkUploadRecordStatus.SUCCESS.name()).operation(WorkflowOperation.INACTIVE.name()).remark(INACTIVE_PENDING_REMARK).masterRecordId(record.getId()).build());
        }
    }

    private BulkUploadRowResult failureResult(String message) {
        String remarkMessage= MDC.get(CORRELATION_ID)+"||"+StringUtils.truncate(message,800);
        return BulkUploadRowResult.builder().recordStatus(BulkUploadRecordStatus.FAILED.name()).remark(remarkMessage).build();
    }



    private List<MasterBulkUploadFileDetail> buildUploadFileDetails(MasterBulkUploadFile uploadFile, List<Map<String, String>> records, List<BulkUploadRowResult> rowResults) {
        List<MasterBulkUploadFileDetail> fileDetails = new ArrayList<>(records.size());
        for (int index = 0; index < records.size(); index++) {
            fileDetails.add(mapDetail(uploadFile, records.get(index), rowResults.get(index)));
        }
        return fileDetails;
    }

    private MasterBulkUploadFileDetail mapDetail(MasterBulkUploadFile uploadFile, Map<String, String> record, BulkUploadRowResult rowResult) {
        MasterBulkUploadFileDetail detail = new MasterBulkUploadFileDetail();
        detail.setMasterUploadFileId(uploadFile.getId());
        detail.setRecordJson(toJson(record));
        detail.setStatus(rowResult.getRecordStatus());
        detail.setOperation(rowResult.getOperation());
        detail.setRemark(rowResult.getRemark());
        detail.setMasterRecordId(rowResult.getMasterRecordId());
        return detail;
    }

    private record CreateMaster(int index, BaseMasterDto dto) {
    }

    private record CreateInactivation(int index, BaseMasterDto record) {
    }
}

-------------------------

@Service
@RequiredArgsConstructor
public class MasterBulkUploadService {
    private final MasterBulkUploadConfigRepository configRepository;
    private final MasterBulkUploadFileRepository fileRepository;
    private final MasterBulkUploadFileDetailRepository fileDetailRepository;
    private final MasterBulkUploadValidation bulkUploadValidation;

    public MasterBulkUploadConfig findConfigByMasterType (String masterCode){
        MasterBulkUploadConfig config= configRepository.findByMasterTypeIgnoreCase(masterCode)
                .orElseThrow(() -> new AdminPortalException(ErrorConstants.NOT_FOUND_ERROR_CODE, MessageFormat.format(ErrorConstants.NOT_FOUND_ERROR_MESSAGE, "Bulk upload config for " + masterCode)));
        if (!config.isBulkUploadAllowed()) {
            throw new ValidationException(ErrorConstants.INVALID_ERROR_CODE, "Bulk upload is not allowed for masterType: " +masterCode);
        }
        return config;
    }


    public MasterBulkUploadFile insertFileRecord(String masterCode, MultipartFile file, String s3Path, UUID masterConfigId, String checksum){
        MasterBulkUploadFile uploadFile = MasterBulkUploadFile.builder().fileName(file.getOriginalFilename()).filePath(s3Path)
                .parsingStatus(BulkUploadParsingStatus.IN_PROGRESS).fileStatus(BulkUploadFileStatus.IN_PROGRESS).remark("File parsing is in progress.")
                .masterConfigId(masterConfigId).masterType(masterCode).fileChecksum(checksum).build();
        return fileRepository.save(uploadFile);
    }


    public List<Map<String, String>> readRecords(MultipartFile file, BulkUploadFormatConfig formatConfig) {
        try (BufferedReader reader = new BufferedReader(new InputStreamReader(file.getInputStream(), StandardCharsets.UTF_8))) {
            List<String> nonBlankLines = reader.lines().map(String::trim).filter(StringUtils::isNotBlank).toList();
            if (nonBlankLines.isEmpty()) {
                throw new ValidationException(ErrorConstants.MANDATORY_ERROR_CODE, "Bulk upload file is empty.");
            }
            List<String> headers = parseCsvLine(nonBlankLines.getFirst());
            // validate Header
            bulkUploadValidation.validateHeaders(headers, formatConfig.getHeaders());
            //  Get List of record.
            List<Map<String, String>> records = new ArrayList<>();
            for (int i = 1; i < nonBlankLines.size(); i++) {
                List<String> values = parseCsvLine(nonBlankLines.get(i));
                if (values.stream().allMatch(StringUtils::isBlank)) {
                    continue;
                }
                Map<String, String> row = new LinkedHashMap<>();
                for (int index = 0; index < headers.size(); index++) {
                    row.put(headers.get(index), index < values.size() ? values.get(index).trim() : null);
                }
                records.add(row);
            }
            return records;
        } catch (IOException e) {
            throw new AdminPortalException(ErrorConstants.GENERIC_ERROR_CODE, ErrorConstants.GENERIC_ERROR_MESSAGE);
        }
    }

    public void saveMasterFileDtls(List<MasterBulkUploadFileDetail> detailEntities){
        fileDetailRepository.saveAll(detailEntities);
    }

    private List<String> parseCsvLine(String line) {
        return java.util.Arrays.stream(line.split(",", -1)).map(String::trim).toList();
    }


    public MasterBulkUploadFile updateFileRecord(MasterBulkUploadFile uploadFile) {
        return fileRepository.save(uploadFile);
    }

    public String generateChecksum(MultipartFile file ) {
        return AdminPortalUtil.generateChecksum(AdminPortalUtil.readFileBytes(file));
    }

    /**
     * Checks whether the same file checksum already exists for the supplied master type.
     *
     * @param checksum file checksum
     * @return {@code true} when a duplicate upload exists
     */
    public boolean existsByFileChecksum( String checksum) {
//        logger.debug("DAO layer: Checking duplicate upload for checksum={}", checksum);
        return fileRepository.existsByFileChecksum( checksum);
    }
}
--------------------------------------

/**
 * Class Name: CountryCurrencyService
 * *
 * Author: V1018841(Saurabh Mahto)
 * <p>
 * Copyright (c) 2026 [State Bank of India]
 * All rights reserved
 * *
 * Version:1.0
 */
@Service("countryCurrencyService")
@RequiredArgsConstructor
public class CountryCurrencyService implements BaseMasterService<CountryCurrencyDto> {

    private final CountryCurrencyValidator countryCurrencyValidator;
    private final CountryCurrencyDao countryCurrencyDao;
    private final ObjectMapper objectMapper;

    @Override
    public MasterType getMasterCode() {
        return MasterType.COUNTRY_CURRENCY;
    }

    @Override
    public List<MasterType> getParentCodes() {
        return List.of();
    }

    @Override
    public Class<CountryCurrencyDto> getDtoClass() {
        return CountryCurrencyDto.class;
    }

    @Override
    public CountryCurrencyDto mapToMasterDto(MasterRequest masterRequest) {
        return objectMapper.convertValue(masterRequest.getData(), CountryCurrencyDto.class);
    }

    @Override
    public MasterWorkFlowDto mapToMasterWorkFlowDto(CountryCurrencyDto dto, WorkflowOperation workflowOperation, String remark) {
        return MasterWorkFlowDto.builder().masterCode(MasterType.COUNTRY_CURRENCY).action(workflowOperation).remark(remark).entityId(dto.getId().toString()).build();
    }

    @Override
    public void validate(CountryCurrencyDto dto, WorkflowOperation workflowOperation) {
        switch (workflowOperation) {
            case CREATE -> countryCurrencyValidator.validateCreate(dto);
            case UPDATE -> countryCurrencyValidator.validateUpdate(dto);
            default ->
                    throw new AdminPortalException(ErrorConstants.INVALID_ERROR_CODE, MessageFormat.format(ErrorConstants.INVALID_ENUM_ERROR_MESSAGE, workflowOperation.name()));
        }
    }

    @Override
    public void validateAction(MasterBulkRequest masters, String actionName) {
        countryCurrencyValidator.validateAction(masters, actionName);
    }

    @Override
    public void validateBulkMasters(List<CountryCurrencyDto> masters, List<String> workflowActions, String performAction) {
        countryCurrencyValidator.validateBulkMasters(masters, workflowActions, performAction);
    }

    @Override
    public CountryCurrencyDto save(CountryCurrencyDto dto, WorkflowOperation action) {
        dto.setStatus(MasterAction.INACTIVE.name());
        dto.setAction(MasterAction.PENDING_FOR_APPROVAL.name());
        switch (action) {
            case CREATE -> {
                return countryCurrencyDao.create(dto);
            }
            case UPDATE -> {
                return countryCurrencyDao.update(dto);
            }
        }
        throw new AdminPortalException(ErrorConstants.INVALID_ERROR_CODE, MessageFormat.format(ErrorConstants.INVALID_ENUM_ERROR_MESSAGE, action.name()));
    }

    @Override
    public void performAction(List<UUID> ids, WorkflowOperation action) {
        switch (action) {
            case APPROVE -> countryCurrencyDao.performAction(ids, MasterAction.APPROVED);
            case REJECT -> countryCurrencyDao.performAction(ids, MasterAction.PENDING_FOR_REJECTED);
            case INACTIVE -> countryCurrencyDao.performAction(ids, MasterAction.PENDING_FOR_INACTIVE);
            default ->
                    throw new AdminPortalException(ErrorConstants.INVALID_ERROR_CODE, MessageFormat.format(ErrorConstants.INVALID_ENUM_ERROR_MESSAGE, action.name()));
        }
    }

    @Override
    public List<CountryCurrencyDto> findByIds(List<UUID> ids) {
        return countryCurrencyDao.findAllById(ids);
    }


    @Override
    public Page<CountryCurrencyDto> search(MasterSearchRequest request, Pageable pageable) {
        Specification<CountryCurrency> specification = MasterSpecification.buildCountryCurrency(request.getFilters());
        return countryCurrencyDao.findBySpecification(specification, pageable);
    }

    @Override
    public Optional<CountryCurrencyDto> findExisting(CountryCurrencyDto dto) {
        return countryCurrencyDao.findCountryCurrency(dto);
    }

    @Override
    public Optional<CountryCurrencyDto> findActiveApproved(CountryCurrencyDto dto) {
        return countryCurrencyDao.findCountryCurrencyDtls(dto);
    }

    @Override
    public List<CountryCurrencyDto> saveAll(List<CountryCurrencyDto> dtos) {
        return countryCurrencyDao.create(dtos);
    }

    @Override
    public AdminPortalResponse<Map<String, Object>> mapToResponse(List<CountryCurrencyDto> dtos, Long total) {
        List<Map<String, Object>> masterResponse = dtos.stream().<Map<String, Object>>map(countryCurrencyDto -> objectMapper.convertValue(countryCurrencyDto, new TypeReference<>() {
        })).collect(Collectors.toList());
        return AdminPortalResponse.<Map<String, Object>>builder().status(SUCCESS_RESPONSE_CODE).data(masterResponse).count((long) dtos.size()).total(total).build();
    }

}
----------------------------

@Component
@RequiredArgsConstructor
public class CountryCurrencyDao {
    private final MasterMapper<CountryCurrencyDto, CountryCurrency> masterMapper;
    private final CountryCurrencyRepository countryCurrencyRepository;

    public CountryCurrencyDto create(CountryCurrencyDto countryCurrencyDto) {
        CountryCurrency entity = countryCurrencyRepository.save(masterMapper.mapDtoToEntity(countryCurrencyDto));
        return masterMapper.mapEntityToDto(entity);
    }

    public CountryCurrencyDto update(CountryCurrencyDto dto) {
        CountryCurrency entity = countryCurrencyRepository.findById(dto.getId()).orElseThrow(() -> new AdminPortalException(FAILED_ERROR_CODE, MessageFormat.format(FAILED_ERROR_MESSAGE_WITH_REASON, "CountryCurrency update", "CountryCurrency record not found for the masterId")));
        masterMapper.updateEntityByDto(dto, entity);
        CountryCurrency updatedCountryCurrency = countryCurrencyRepository.save(entity);
        return masterMapper.mapEntityToDto(updatedCountryCurrency);
    }

    public List<CountryCurrencyDto> findAllById(List<UUID> ids) {
        return countryCurrencyRepository.findAllById(ids).stream().map(masterMapper::mapEntityToDto).toList();
    }

    public List<CountryCurrencyDto> create(List<CountryCurrencyDto> dtos) {
        dtos.forEach(dto -> {
            dto.setStatus(MasterAction.INACTIVE.name());
            dto.setAction(MasterAction.PENDING_FOR_APPROVAL.name());
        });
        List<CountryCurrency> entities = masterMapper.mapDtoToEntity(dtos);
        List<CountryCurrency> savedEntities = countryCurrencyRepository.saveAll(entities);
        return masterMapper.mapEntityToDto(savedEntities);
    }


    public void performAction(List<UUID>ids, MasterAction action){
        switch(action){
            case APPROVED ->countryCurrencyRepository.approve(ids, MasterAction.APPROVED.name());
            case PENDING_FOR_REJECTED -> countryCurrencyRepository.performAction(ids, MasterAction.PENDING_FOR_REJECTED.name());
            case PENDING_FOR_INACTIVE -> countryCurrencyRepository.performAction(ids, MasterAction.PENDING_FOR_INACTIVE.name());
        }
    }

    public Page<CountryCurrencyDto> findBySpecification(Specification<CountryCurrency> specification, Pageable pageable) {
        Page<CountryCurrency> page = countryCurrencyRepository.findAll(specification, pageable);
        return page.map(masterMapper::mapEntityToDto);
    }


    public Optional<CountryCurrencyDto> findCountryCurrencyDtls(CountryCurrencyDto dto){
        Optional<CountryCurrency> countryCurrency= countryCurrencyRepository.findByCodeAndNameAndCurrencyCodeAndCurrencyNameAndStatusAndAction(
                dto.getCode(), dto.getName(), dto.getCurrencyCode(), dto.getCurrencyName(), "ACTIVE", "APPROVED");
        return countryCurrency.map(masterMapper::mapEntityToDto);
    }

    public Optional<CountryCurrencyDto> findCountryCurrency(CountryCurrencyDto dto){
        return countryCurrencyRepository.findByCodeAndNameAndCurrencyCodeAndCurrencyName(
                dto.getCode(), dto.getName(), dto.getCurrencyCode(), dto.getCurrencyName()).map(masterMapper::mapEntityToDto);
    }


}
