package com.epay.merchant.mapper;

import com.epay.merchant.dto.MerchantOnboardBankAccountDto;
import com.epay.merchant.dto.MerchantOnboardingDto;
import com.epay.merchant.model.request.OnboardingRequest;
import com.epay.merchant.model.response.MerchantBankAccResponse;
import org.mapstruct.Builder;
import org.mapstruct.Mapper;
import org.mapstruct.Mapping;
import org.mapstruct.Mappings;

import java.util.List;

@Mapper(builder = @Builder(disableBuilder = true), componentModel = "spring")
//@Mapper(componentModel = "spring", unmappedTargetPolicy = ReportingPolicy.IGNORE)
public interface OnboardingMapper {


        @Mapping(source = "merchant", target = "merchant")
        @Mapping(source = "user", target = "user")
        @Mapping(source = "bankAccounts", target = "bankAccounts")
        OnboardingRequest toOnboardingRequest(MerchantOnboardingDto dto);

        List<MerchantOnboardBankAccountDto> toBankAccountList(List<MerchantBankAccResponse> list);

        @Mappings({
//                @Mapping(source = "mId", target = "merchantId"),
                @Mapping(source = "accHolderName", target = "accHolderName"),
//                @Mapping(source = "accountUniqueIdentifier", target = "accUniqueIdentifier"),
//                @Mapping(source = "accountNumber", target = "accountNumber"),
//                @Mapping(source = "accountType", target = "accountType"),
//                @Mapping(source = "bankName", target = "bankName"),
//                @Mapping(source = "ifscCode", target = "ifscCode"),
//                @Mapping(source = "branchName", target = "branch"),
//                @Mapping(source = "approved", target = "approved"),
//                @Mapping(source = "primary", target = "primary")
        })
        MerchantOnboardBankAccountDto toBankAccount(MerchantBankAccResponse dto);

}
-------------
package com.epay.merchant.dto;

import com.epay.merchant.model.response.MerchantBankAccResponse;
import com.epay.merchant.model.response.MerchantBankAccountResponse;
import lombok.AllArgsConstructor;
import lombok.Builder;
import lombok.Data;
import lombok.NoArgsConstructor;

import java.util.List;

/**
 * Class Name: MerchantOnboardingDto
 * *
 * Description:Represents a request for onboarding a new merchant and user, typically used during the registration process.
 * *
 * Author: Rahul Prajapati
 * <p>
 * Copyright (c) 2026 [State Bank of India]
 * All rights reserved
 * *
 * Version:1.0
 */
@Data
@NoArgsConstructor
@Builder
@AllArgsConstructor
public class MerchantOnboardingDto {
   private MerchantDto merchant;
   private MerchantUserDto user;
   private List<MerchantBankAccResponse> bankAccounts;

}
-----------------------
package com.epay.merchant.model.response;

import lombok.AllArgsConstructor;
import lombok.Data;
import lombok.NoArgsConstructor;
import lombok.ToString;

@AllArgsConstructor
@Data
@ToString
@NoArgsConstructor
public class MerchantBankAccResponse {
    private String merchantId;
    private String accHolderName;
    private String accUniqueIdentifier;
    @ToString.Exclude
    private String accountNumber;
    private String accountType;
    private String bankName;
    private String ifscCode;
    private String branch;
    private String branchCode;
    private boolean isApproved;
    private boolean isPrimary;
}
------------------------------
@Service
@RequiredArgsConstructor
public class AdminService {
 private final LoggerUtility log = LoggerFactoryUtility.getLogger(this.getClass());
    private final OnboardingValidator onboardingValidator;
    private final AdminValidator adminValidator;
    private final MerchantUserValidator mIdValidator;
    private final MerchantEntityGroupMapper merchantEntityGroupMapper;
    private final MerchantMapper merchantMapper;
    private final HelpSupportService helpSupportService;
    private final MerchantBankService merchantBankService;
    private final AdminDao adminDao;
    private final ObjectMapper objectMapper;
    private final TokenService tokenService;
    private final OnboardingMapper onboardingMapper;

    /***
     * Fetching merchantDetails on the basis of mId
     * @param mId String
     */
    public void  onboardAdminPortalMerchant(String mId){
        log.info("Processing merchant onboarding request for mid : {}",mId);
        MerchantResponse<MerchantOnboardingDto> adminMerchantInfoDto = adminDao.getMerchantsDetailsByMId(mId);
        MerchantOnboardingDto onboardingDto = adminMerchantInfoDto.getData().getFirst();
        OnboardingRequest onboardingRequest = onboardingMapper.toOnboardingRequest(onboardingDto);
        onboardingMerchant(onboardingRequest);
        log.info("Completed merchant onboarding for mid : {}",mId);
    }
}
}
====================
public class AdminPortalServicesClient extends ApiClient {
    public static final String LOG_EXCEPTION = "Got exception {}";
    private final LoggerUtility logger = LoggerFactoryUtility.getLogger(this.getClass());   

public MerchantResponse<MerchantOnboardingDto> getMerchantOnboardDetails(String mId) {
        return getMerchantResponse(MERCHANT_ONBOARDING_ENDPOINT.formatted(mId), new ParameterizedTypeReference<>() {});
    }
    /**
     *
     * @param urlPath String
     * @param typeReference ParameterizedTypeReference of  MerchantResponse
     * @return MerchantResponse
     * @param <T> MerchantResponse
     */
    protected <T> MerchantResponse<T> getMerchantResponse(String urlPath, ParameterizedTypeReference<MerchantResponse<T>> typeReference) {
        try {
            URI uri = URI.create(getBaseUrl() + urlPath);
            logger.info("Get call URI: {}", uri);
            MerchantResponse<T> merchantResponse =
                    getWebClient()
                            .get()
                            .uri(uri)
                            .headers(httpHeaders -> httpHeaders.addAll(prepareHttpHeaders()))
                            .exchangeToMono(response -> response.bodyToMono(typeReference))
                            .block();
            validateResponse(merchantResponse);
            return  merchantResponse;
        } catch (NestedRuntimeException e) {
            logger.error(LOG_EXCEPTION, e.getMessage());
            throw new MerchantException(EXTERNAL_SERVICE_ERROR_CODE, MessageFormat.format(EXTERNAL_SERVICE_ERROR_MESSAGE, getServiceName()));
        }
    }
