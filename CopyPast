@Test
    void downloadMerchantOrderPaymentReport_WithEmptyMerchantId() {
        // Arrange
        String emptyMerchantId = "";
        Page<OrderResponse> orderPage = new PageImpl<>(Collections.singletonList(testOrderResponse), testPageable, 1);
        when(transactionConfig.getReportPageSize()).thenReturn(1000);
        when(merchantOrderPaymentDao.findBySpecification(eq(emptyMerchantId), any(MerchantOrderPaymentSearchRequest.class),anyString(), any(Pageable.class))).thenReturn(orderPage);
//        when(fileGeneratorService.buildFileModel(any(ReportFormat.class), anyList(), anyList(), anyMap())).thenReturn(new FileModel());

        // Act
        orderService.downloadMerchantOrderPaymentReport(response, emptyMerchantId,anyString(), testSearchRequest);

        // Assert
        verify(orderValidator, times(1)).validateMerchantOrderPaymentsRequest(eq(testSearchRequest), eq(emptyMerchantId), any(Pageable.class), eq(true));
        verify(merchantOrderPaymentDao, times(1)).findBySpecification(eq(emptyMerchantId), any(MerchantOrderPaymentSearchRequest.class),anyString(), any(Pageable.class));
    }



Invalid use of argument matchers!
0 matchers expected, 1 recorded:
-> at com.epay.transaction.service.OrderServiceTest.downloadMerchantOrderPaymentReport_WithEmptyMerchantId(OrderServiceTest.java:477)

This exception may occur if matchers are combined with raw values:
    //incorrect:
    someMethod(any(), "raw String");
When using matchers, all arguments have to be provided by matchers.
For example:
    //correct:
    someMethod(any(), eq("String by matcher"));

For more info see javadoc for Matchers class.

org.mockito.exceptions.misusing.InvalidUseOfMatchersException: 
Invalid use of argument matchers!
0 matchers expected, 1 recorded:
-> at com.epay.transaction.service.OrderServiceTest.downloadMerchantOrderPaymentReport_WithEmptyMerchantId(OrderServiceTest.java:477)

This exception may occur if matchers are combined with raw values:
    //incorrect:
    someMethod(any(), "raw String");
When using matchers, all arguments have to be provided by matchers.
For example:
    //correct:
    someMethod(any(), eq("String by matcher"));

For more info see javadoc for Matchers class.

	at com.epay.transaction.config.TransactionConfig.getReportPageSize(TransactionConfig.java:37)
	at com.epay.transaction.service.OrderService.downloadMerchantOrderPaymentReport(OrderService.java:187)
	at com.epay.transaction.service.OrderServiceTest.downloadMerchantOrderPaymentReport_WithEmptyMerchantId(OrderServiceTest.java:477)
	at java.base/java.lang.reflect.Method.invoke(Method.java:580)
	at java.base/java.util.ArrayList.forEach(ArrayList.java:1596)
	at java.base/java.util.ArrayList.forEach(ArrayList.java:1596)



> Task :test
OrderServiceTest > downloadMerchantOrderPaymentReport_WithEmptyMerchantId() FAILED
    org.mockito.exceptions.misusing.InvalidUseOfMatchersException at OrderServiceTest.java:477
1 test completed, 1 failed
